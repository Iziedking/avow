[package]
name = "avow"
edition = "2024"

# The Sui framework and standard library are implicit system dependencies, so no [dependencies]
# block is required. testnet and mainnet are built-in system environments on this toolchain, so
# no [environments] block is needed (and declaring them errors). The package's own named address
# (avow) binds automatically from the package name, so no [addresses] section is needed either.
